import type { FunctionComponent } from "react";

interface IProps {}

const ToggleArrowIcon: FunctionComponent<IProps> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Vars ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  // ~~~~~ JSX ~~~~~
  return (
    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="10" fill="none">
      <path
        stroke="#666"
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth="2.25"
        d="m16.488 1.139-7.5 7.5-7.5-7.5"
      ></path>
    </svg>
  );
};

export default ToggleArrowIcon;
